import { templates } from '../../layouts/templates.ts';
import type {ILayoutPickerProps} from '../../types/types.ts';

const LayoutPicker = ({layout, setLayout, templates}: ILayoutPickerProps) => {
  return (
    <div>10</div>
  )
}

export default LayoutPicker